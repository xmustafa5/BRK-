export default function SectionTitle({ title }) {
  return (
    <h2
      className={`bold boldAr text-black-120 text-[28px] sm:text-[35px] font-semibold tracking-normal `}
    >
      {title}
    </h2>
  );
}
